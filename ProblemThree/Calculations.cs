namespace ProblemThree
{
    public class Calculations
    {
        // минимальное время выполнения
        public int MinimalCompletionTime(double[] sourceQueue, int processorCount, 
                                         int processCount, int copyCount)
        {
            return 0;
        }
    }
}